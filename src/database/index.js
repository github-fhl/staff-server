const
  {dumpDatabase} = require('./dumpDatabase'),
  {importDatabase} = require('./importDatabase'),
  {getList} = require('./getList')

module.exports = {
  dumpDatabase,
  importDatabase,
  getList
}
